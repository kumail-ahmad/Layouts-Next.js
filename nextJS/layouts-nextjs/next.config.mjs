/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["dummyimage.com", "https://imgs.search.brave.com/"],
  },
};

export default nextConfig;
